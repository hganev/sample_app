<!DOCTYPE html>
<html>
<head>
  <title>RoR tutorial Sample App | <%= title %></title>
	<%= render 'layouts/stylesheets' %>
</head>
<body>
<div class="container">
	<header>
	<%= render 'layouts/header' %>
	<section class="round">
		<% flash.each do |key, value| %>
			<div class="flash <%= key %>"><%= value %></div>
		<% end %>
		<%= yield %>
	</section>
	</header>
	<%= render 'layouts/footer' %>
  <%= debug(params) if Rails.env.development? %>
</div>
</body>
</html>
